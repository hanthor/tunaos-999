# Triage Labels

The skills speak in terms of five standard roles for triage. This file maps each role to the label string that this repo's issue tracker uses.

| Label in mattpocock/skills | Label in our tracker | Meaning                                  |
| -------------------------- | -------------------- | ---------------------------------------- |
| `needs-triage`             | `needs-triage`       | Maintainer needs to evaluate this issue  |
| `needs-info`               | `needs-info`         | Waiting on reporter for more information |
| `ready-for-agent`          | `ready-for-agent`    | Fully specified, ready for an AFK agent  |
| `ready-for-human`          | `ready-for-human`    | Requires human implementation            |
| `wontfix`                  | `wontfix`            | Will not be actioned                     |

When a skill mentions a role (e.g. "apply the AFK-ready triage label"), use the corresponding label string from this table.

Edit the right-hand column to match the words your team uses.

For the policy of the whole queue, see [`TRIAGE-POLICY.md`](../../TRIAGE-POLICY.md). It gives the SLA tiers, the rule for an issue that a bot filed, and the signal that separates a milestone from the backlog. This file maps the labels of one issue only.
